# Designs (Phase 2 placeholder)

Wireframes and design references go here in Phase 2. Today this folder is empty by intent so that:

1. A future Design Reader agent has a known location to inspect.
2. The `simplelife-agents` workspace can reference wireframes alongside code changes without external dependencies.

Suggested formats: PNG/SVG screenshots, optional Figma export links in `designs/README.md`.
